class deed {
  constructor ( data ){
    this.const = {
      index: data.index
    };
    this.var = {
      archetype: data.archetype,
      subtype: data.subtype
    };
    this.array = {
    };
  }

  draw(  ){
  }
}
